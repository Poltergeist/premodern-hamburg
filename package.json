{
  "name": "premodern.hamburg",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest run",
    "test:watch": "vitest",
    "sync": "tsx scripts/sync-events.ts",
    "sync:decklists": "tsx scripts/sync-decklists.ts"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "astro": "^6.1.1"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "vitest": "^4.1.2"
  }
}
